# Release Candidate Notes - 2026-06-25

## Current State

This is the first production push for this repository. `origin/master` was last updated through `62dd339` (2026-06-23); local `master` is 150 commits ahead, all the way back through Specs 001-016, the full Spec 017 external-data verification (48/48 teams), and Spec 018 Phases 0-7. `git merge-base --is-ancestor origin/master HEAD` confirms this is a clean fast-forward -- no divergence, no conflicting remote edits.

Recent key commits (Spec 017/018, this handoff):

- `cf50c5b` / `483b765` - Spec 017 complete: external data verification for all 48 World Cup teams, 346 URL-backed candidates
- `0c391ce` - source traceability repaired for the original 16-team batch (121 -> 1 candidates missing a resolvable URL)
- `c07dfcc` - 4 data-change proposal reports built (current-field/rating/tactical/roster), nothing applied yet
- `adb1ff7` - exactly one safe, verified factual update applied: Uruguay fifa_rank 14 -> 16; two genuinely conflicting/overtaken candidates held for Codex (Tunisia manager identity, Ghana fifa_rank)
- `1be4080` - manager substitution profile prototype added to the match engine (neutral-default-preserving, fully tested, no real per-team data yet)
- `0ff1dd6` - full diagnostics suite re-run after the above; confirmed no regression (Uruguay's change affects only its own 19 matchups, overall benchmark stats unchanged)
- `6aa6b7d` - fixed a substitution-model-gap diagnostic that the Phase 5 prototype itself had made stale
- `1fcef7d` - `CURRENT_TASK.md` marked complete with explicit Codex handoff notes

## Accuracy Impact

No formula, rating, or tactical-weight change in this handoff. The only seed-data change is Uruguay's `fifa_rank` (14 -> 16, Tier S), verified to have zero measurable effect on the overall benchmark summary (avg/min/max favorite-win-pct and implausible-favorite count are byte-identical with the old value held constant; only Uruguay's own 19 matchups shift by 1-2 points each).

The substitution-profile prototype is also a zero-behavior-change addition: every pre-existing test passes unchanged, confirming the neutral default reproduces the original fatigue-only substitution logic exactly (including identical RNG consumption).

One pre-existing calibration-transparency item surfaced during this handoff, not caused by it: `prediction_benchmark_baseline_2026-06-25.json` is the first report generated by the benchmark script's already-installed dual-order-averaging fix, and shows `implausible_favorite_count=43` rather than the stale `2026-06-23` report's single-order-biased `27`. This falls inside the 27-57 range a prior session had already predicted once dual-order averaging was applied. The separately-tracked `prediction_benchmark_comparison_rank75_order_neutral_2026-06-23.json` (which actually gates `readyForManualPush`) still reports `status: pass` for the rank75-over-v1 improvement itself -- that comparison is unaffected.

## Release Blockers

None remaining. `docs/specs/CURRENT_TASK.md` was updated to a completed/awaiting state (commit `1fcef7d`) specifically to clear the one structural blocker `build_release_readiness_report.py` was flagging.

## Final Local Gate

```powershell
.\scripts\pre_release_check.ps1
cd backend
.\venv\Scripts\python.exe scripts\build_release_readiness_report.py
```

Result (2026-06-25):

- `readyForManualPush=true`
- `blockers=[]`
- `gitStatusShort=[]`
- rank75 benchmark status `pass` (`prediction_benchmark_comparison_rank75_order_neutral_2026-06-23.json`)
- backend `pytest`: 387 passed
- frontend `lint`/`build`: clean
- `audit_text_encoding.py`: passed

## Production Target

- Branch: `master`
- Frontend: `https://wc2026-sim-ten.vercel.app` (Vercel, auto-deploys on push to `master`)
- Backend: `https://wc2026-backend-tdih.onrender.com` (Render, auto-deploys on push to `master`; SQLite re-seeds from `backend/data/seed/*.json` on a fresh container since the filesystem is ephemeral)

## Post-Deploy Gate

```powershell
.\scripts\post_deploy_smoke.ps1 -FrontendBaseUrl "https://wc2026-sim-ten.vercel.app" -BackendBaseUrl "https://wc2026-backend-tdih.onrender.com"
```
