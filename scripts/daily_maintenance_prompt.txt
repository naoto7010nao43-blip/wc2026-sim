Daily maintenance run for the WC2026 football sim web app at C:\Users\naoto\Documents\myclaude.ai.project\wc2026-sim (backend: FastAPI+SQLAlchemy+SQLite at backend/, frontend: React+Vite+TS+Tailwind at frontend/). This app predicts the 2026 FIFA World Cup for a Japanese audience (analysts + casual fans), and reflects REAL match results for already-played fixtures (backend/data/seed/real_results/{A..L}.json, one file per group, loaded live by backend/app/services/real_results.py -- no reseed needed when only these files change) while simulating fixtures not yet played. Dev servers may or may not already be running (check `netstat -ano | grep ":8000"` / `":5173"` before assuming, on Windows via PowerShell Get-NetTCPConnection or netstat).

This app is PUBLICLY DEPLOYED and shared with real users: frontend at https://wc2026-sim-ten.vercel.app/, backend at https://wc2026-backend-tdih.onrender.com. The repo is at https://github.com/naoto7010nao43-blip/wc2026-sim (local remote `origin`, branch `master`). Pushing to `master` auto-deploys both sides (Render rebuilds the backend into a fresh container and auto-reseeds its SQLite DB from data/seed/*.json on startup since the filesystem is ephemeral; Vercel rebuilds the frontend). Treat the production site, not just localhost, as the real target when checking whether something needs fixing -- e.g. the design department should spot-check the live URL with Playwright, not only the dev server.

Run these 4 work streams. Use the Agent tool to spawn them as separate subagents so they run in parallel where independent; keep each one's scope bounded as described -- this is a recurring daily job, not a one-time overhaul.

1. Data update department (real match results): For each of the 12 groups (A-L), check whether new matches have been played since the data was last updated. A structured provider (API-Football) was evaluated for this but its free tier doesn't cover the current World Cup season (or even meaningful per-player statistics for older seasons -- confirmed by direct API testing, not assumption) and the paid tier wasn't adopted, so this stays WebSearch/WebFetch-based -- but follow the verification discipline learned the hard way this session:
   - Cross-check every score/scorer/date against at least 2 independent sources (Wikipedia + one of ESPN/BBC/FIFA.com) before writing it down. Never report a stat you can't point to in literally-fetched text.
   - Team-level stats (possession, shots, shots on target, cards) are fine to research the same way -- they're simple, low-granularity numbers that are easy to cross-verify.
   - Do NOT attempt full starting lineups or per-player match ratings via WebSearch/WebFetch -- this was tried twice with increasingly careful methodology and still surfaced an implausible, unverifiable detail (a fabricated-looking red-card incident) that recurred independently. Player-level rating display for real matches deliberately stays the conservative goal-scorer-only estimate in player_ratings.estimate_real_match_ratings -- do not try to replace it with researched per-player data.
   - Append new matches to backend/data/seed/real_results/{X}.json following the existing schema (home_team_id, away_team_id, scores, date, goals[] with scorer_name + scorer_name_ja, stats{}). Never duplicate or remove existing entries. After any edit, run a goal-tally sanity check (sum of each team's goal events must equal home_score/away_score -- this caught a real data bug once before).

2. Engineering QA department: Run `cd backend && ./venv/Scripts/python.exe -m pytest -q` and `cd frontend && npx tsc --noEmit`. If anything fails, investigate and fix it (small, targeted fixes only -- do not refactor broadly). Also sanity-check that both dev servers actually respond (curl localhost:8000/api/teams, curl localhost:5173) and restart any that are down or stale (kill the PID via PowerShell Stop-Process -Force, relaunch uvicorn/vite).

3. Site design department: Look at the live site (start/use the dev servers, navigate with Playwright -- install via `npm install playwright` + `npx playwright install chromium` in a temp dir if not already available, take screenshots). Pick ONE small, concrete visual/UX rough edge (spacing, contrast, a confusing label, a missing loading/empty state, mobile layout glitch, etc.) and fix it. Verify the fix with a before/after screenshot and check for zero console errors. Do not attempt a broad redesign in a single run.

4. Data analyst department (realism refinement): Pick 3-6 players or 1-2 teams whose data (qualitative_adjustments in backend/data/seed/players.json, or tactical_profile/manager_name in teams.json) looks stale or wrong given recent real-world news (injuries, suspensions, manager changes, current form, transfer moves, club-season performance), research via WebSearch, and make small bounded corrections with a brief rationale in source_citations. Ground these adjustments in concrete numbers where possible -- a player's current club-season totals (league goals/assists/appearances, e.g. from Transfermarkt or worldfootball.net, which are simple static pages that WebFetch can verify reliably, unlike JS-heavy rating sites) are a more defensible basis for a qualitative bump than vague "good form" impressions. If you touch data/seed/*.json, you MUST run `cd backend && ./venv/Scripts/python.exe scripts/seed_db.py` afterward and restart the backend uvicorn process for it to take effect.

After all 4 streams finish: if backend data/seed files changed, confirm seed_db.py was re-run and the backend was restarted locally; run the pytest suite (`pytest -q`) and the frontend typecheck (`npx tsc --noEmit`) one final time to confirm everything is green; leave both dev servers running.

Then publish: if `git status --short` shows any changes from the 4 streams, stage them, commit (one commit is fine, message summarizing what changed), and push to `origin master` so production picks it up. Authenticate with: `$env:GH_TOKEN = [Environment]::GetEnvironmentVariable('GH_TOKEN','User'); git push https://x-access-token:$env:GH_TOKEN@github.com/naoto7010nao43-blip/wc2026-sim.git master`. Do not push if pytest or tsc failed -- fix or revert first. After pushing, wait a few minutes and curl https://wc2026-backend-tdih.onrender.com/api/teams and https://wc2026-sim-ten.vercel.app/ to confirm both redeployed successfully (200 responses) before finishing.

Write a short end-of-run summary (bullet list, what changed in each department, whether it was pushed to production, under 200 words total) as your final message.
